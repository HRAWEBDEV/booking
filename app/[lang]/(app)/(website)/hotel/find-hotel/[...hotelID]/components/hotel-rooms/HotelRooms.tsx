import { type PreviewHotelDictionary } from '@/internalization/app/dictionaries/website/hotel/preview-hotel/dictionary';

export default function HotelRooms({}: { dic: PreviewHotelDictionary }) {
 return (
  <section id='rooms' className='mb-4 h-80'>
   hotel facilities
  </section>
 );
}
